# Hello Partner

Minimal Node.js example for the Min Partner API.

```bash
API_BASE=https://api.aare.cc PARTNER_API_KEY=ema_pk_... node index.mjs
```

Calls `GET /v1/partner/me`, `GET /v1/partner/stats`, and creates a test user.
